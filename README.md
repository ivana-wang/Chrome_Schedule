# Chrome_Schedule

Chromebook release-schedule spec and supporting validation material.

- `Chrome_Spec.md` — the specification.
- `Validation/` — source data and reference images (Excel schedule, chromiumdash
  screenshots, product spec, examples) used to validate the spec.
